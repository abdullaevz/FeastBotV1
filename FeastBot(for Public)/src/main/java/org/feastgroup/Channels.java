package org.feastgroup;

/**
 * This enum helps to use all channels with checking.
 * If this enum doesn't exist, then need to add all channels id on methods one by one.
 */
public enum Channels {
    ADVICE_CHANNEL, WARN_INFO_CHANNEL, JOIN_INFO_CHANNEL, READY_INFO_CHANNEL
}
